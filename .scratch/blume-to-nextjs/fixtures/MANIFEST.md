# Fixture manifest — pre-cutover agent surface (Stage 0, Task 0.4)

**Captured:** 2026-09-19 (~12:11 UTC), against production.

**`main` commit SHA live at capture time:** `865bb7706855330b9b4ded5805867a3d56030170`
(short: `865bb77`). This is the commit the controller confirmed as deployed
and serving production before capture began (three pre-ship commits: Tasks
0.1-0.3).

**Purpose:** these 74 files are the **pre-cutover** baseline for §17.4.
Stage 8 diffs the new (Next.js) site's generated text surface against this
exact fixture set. This fixture set and the Blume build it was generated by
both **stop existing the moment Blume is removed (Stage 10)** — which is why
they are frozen here, before that happens.

## Sources (exact URLs)

Root endpoints, captured with `curl -sS --compressed <url> -o <file>` (bytes
written as-is — no normalization, reformatting, or re-encoding):

- `https://sevenui.dev/llms.txt` → `llms.txt`
- `https://sevenui.dev/llms-full.txt` → `llms-full.txt`
- `https://sevenui.dev/sitemap.xml` → `sitemap.xml`
- `https://sevenui.dev/robots.txt` → `robots.txt`
- `https://sevenui.dev/index.md` → `index.md`
- `https://sevenui.dev/agent-readability.json` → `agent-readability.json`

Per-page `.md` mirrors, one per route derived from the `apps/web/docs` and
`apps/web/docs/components` MDX corpus (rule: `<route>.md`; `/docs/index.md`
does not exist, per §15.2), fetched as `https://sevenui.dev<route>.md` →
`md/<route>.md`. 68 routes, 68 files captured.

## Step 4 sanity check — actual vs. expected

All values below are the **actual measured bytes**, checked adversarially
against the brief's approximate expectations. Every assertion matched.

| File | Expected (approx.) | Actual (bytes) | Match |
|---|---:|---:|---|
| `llms.txt` | 9.3K | 9299 | yes |
| `llms-full.txt` | 297K | 297404 | yes |
| `sitemap.xml` | 5.7K | 5737 | yes |
| `robots.txt` | 120 | 120 | yes (exact) |
| `index.md` | 9.3K | 9299 | yes |
| `agent-readability.json` | 565 | 565 | yes (exact) |

Other Step 4 assertions:

- `cmp llms.txt index.md` → **identical**, exit 0 (expected per §15.2 — no
  recapture needed).
- `grep -c '<loc>' sitemap.xml` → **85** (expected: 85, match).
- `grep -c '^Content-Signal' robots.txt` → **1** (expected: 1, match).
- `find md -name '*.md' -size -200c` → **no output** — no truncated mirror
  found in the 68-file set.
- `find md -name '*.md' | wc -l` → **68** (expected: 68, match).

### `.md` mirror size range

- Smallest: `md/docs/components/spinner.md` — 727 bytes.
- Next smallest: `md/docs/components/skeleton.md` — 757 bytes.
- Largest: `md/docs/components/sidebar.md` — 10815 bytes.
- Next largest: `md/docs/components/command.md` — 10000 bytes.

All 68 files are well above the 200-byte truncation threshold; none are
empty or error-page-sized.

## Tree contents

- 6 root endpoint files (`llms.txt`, `llms-full.txt`, `sitemap.xml`,
  `robots.txt`, `index.md`, `agent-readability.json`)
- 68 `.md` files under `md/docs/**`
- This `MANIFEST.md`

Total: 74 fixture files + manifest, verified by `find . -type f | wc -l`.

## Note on this manifest's relationship to Stage 8

This is the **pre-cutover baseline for §17.4** — Stage 8 diffs the Next.js
build's generated agent-facing text surface (llms.txt, llms-full.txt,
sitemap.xml, robots.txt, index.md, agent-readability.json, and the 68 `.md`
route mirrors) against these exact files. Any difference Stage 8 finds is
either an intentional, documented change from the migration plan, or a
regression to fix before shipping.
