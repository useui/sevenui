---
title: Breadcrumb
description: Displays the current location within a hierarchical navigation structure.
---

<Component path="breadcrumb/breadcrumb-demo" />

## Installation

```bash
npx shadcn@latest add @sevenui/breadcrumb
```

## Usage

```tsx
import {
  Breadcrumb,
  BreadcrumbList,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@/components/ui/breadcrumb";

<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink href="/">Home</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbLink href="/components">Components</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbPage>Breadcrumb</BreadcrumbPage>
    </BreadcrumbItem>
  </BreadcrumbList>
</Breadcrumb>;
```

## Examples

### Custom separator

`BreadcrumbSeparator` renders a chevron by default; pass children to
swap it.

<Component path="breadcrumb/breadcrumb-separator" />

## API reference

### BreadcrumbLink

Renders an `a` by default; swap the element with the Base UI `render`
prop to integrate a router link, e.g.
`render={<Link href="/docs" />}`.

| Prop     | Type                 | Default |
| -------- | -------------------- | ------- |
| `render` | `React.ReactElement` | `a`     |

### Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbPage, BreadcrumbSeparator, BreadcrumbEllipsis

Semantic wrappers extending `nav` (labeled "breadcrumb"), `ol`, `li`,
and `span`. `BreadcrumbPage` marks the current page with
`aria-current="page"`; the separator (chevron by default) and ellipsis
take custom children and are hidden from assistive tech.
