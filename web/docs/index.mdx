---
title: Introduction
description: Base UI powered components, distributed through the shadcn registry.
---

SevenUI is a registry of 50 components built exclusively on
[Base UI](https://base-ui.com) primitives. Components are installed as
source code into your project through the shadcn CLI — no runtime
package to depend on.

## Why SevenUI

- **Base UI only** — no Radix. Every component is built on
  [Base UI](https://base-ui.com) primitives, with nothing else underneath.
- **Drop-in shadcn compatibility** — works with the `shadcn` CLI and your
  existing `components.json` setup.
- **Source distribution via the registry** — components install as
  source files you own, not a package you depend on.

## Component coverage

SevenUI ships with 50 components across four waves:

- **Foundation wave** (15): button, badge, kbd, card, alert, separator,
  skeleton, spinner, aspect-ratio, textarea, table, breadcrumb, pagination,
  avatar, progress.
- **Form wave** (15): input, label, field, form, form-rhf, checkbox,
  radio-group, switch, toggle, toggle-group, slider, number-field,
  input-otp, select, combobox.
- **Overlay wave** (11): dialog, alert-dialog, sheet, drawer, popover,
  hover-card, tooltip, dropdown-menu, context-menu, menubar, toast.
- **Navigation & composite wave** (9): tabs, accordion, collapsible,
  navigation-menu, scroll-area, toolbar, meter, command, sidebar.

Navigation and composition are complete — tabs, menus, a shared-popup
navigation menu, a command palette, and a full sidebar system — with
zero third-party UI libraries. No Radix, no cmdk, no vaul, no sonner:
every primitive underneath is Base UI.

Form components use Base UI-native form handling, with an optional
`form-rhf` integration for [react-hook-form](https://react-hook-form.com).

Ready to add your first component? Head over to
[Installation](/installation).
